/* const BBDD = {
    usuario: "Homero",
    constraeña: "1234"
};

const authService = {
    verificarCredenciales: (usuario, constraseña) => {
        //Lógica de verificación
        return usuario === BBDD.usuario && constraseña === BBDD.constraseña;        
    },
    generarToken: (usuario) => {
        //Generación de Token con libreria: jsonwebtoken
        const token = "tokenGenerado";
        return token;
    }
};

module.exports = authService; */